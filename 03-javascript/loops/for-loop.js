function ent()
{
    var k;
    table = 9;

    

    document.write("The Table for " + table + "<br>");
    for(k=1; k<=10; k++)
        {
        
        document.write(k*table + "<br>");
       }

}